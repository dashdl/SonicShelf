<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhongxin.sonicshelf.mapper.DynamicMapper">

    <select id="selectAllByUserId" resultType="com.zhongxin.sonicshelf.dto.response.DynamicResponse">
        select
        d.id,
        d.user_id,
        d.target_id,
        d.type,
        u.avatar,
        u.nickname as user_name,
        d.created_at,
        d.content,
        d.like_count,
        d.share_count,
        d.comment_count
        from dynamics as d
        join users as u on d.user_id = u.id
        where d.user_id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        or d.user_id =#{currentUserId}
        order by d.created_at desc
    </select>
    <select id="selectByUserId" resultType="com.zhongxin.sonicshelf.dto.response.DynamicResponse">
        select d.id,
               d.user_id,
               d.target_id,
               d.type,
               u.avatar,
               u.nickname as user_name,
               d.created_at,
               d.content,
               d.like_count,
               d.share_count,
               d.comment_count
        from dynamics as d
                 join users as u on d.user_id = u.id
        where d.user_id = #{userId}
        order by d.created_at desc
    </select>

</mapper>